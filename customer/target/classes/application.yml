server:
  port: 8080
spring:
  application:
    name: customer
  datasource:
    username: 'dimuthu'
    url: jdbc:postgresql://localhost:5432/customer
    password: '12345678'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: 'true'
    hibernate:
      ddl-auto: create
    show-sql: 'true'
